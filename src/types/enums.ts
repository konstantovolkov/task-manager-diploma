export enum IconType {
  EMAIL = 'email',
  USER = 'user',
  PASSWORD = 'password'
}
export enum InputType {
  EMAIL = 'email',
  TEXT = 'text',
  PASSWORD = 'password'
}

export enum FormType {
  LOGIN = 'Sign in',
  REGISTER = 'New account'
}
